<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (products.length > 0) { %>
                <ul>
                    <% products.forEach(p => { %>
                        <li style="margin: 10px;">
                            <img src="<%= p.productData.imageUrl %>" style="width:70px;height:70px;margin: 5px;" />
                            <span style="margin: 5px;"><%= p.productData.title %> (<%= p.qty %>)</span>
                            <span style="margin: 10px;">$<%= p.productData.price * p.qty %></span>
                            <form action="/cart-delete-item" method="POST">
                                <input type="hidden" value="<%= p.productData.id %>" name="productId">
                                <button class="btn" type="submit">Delete</button>
                            </form>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <h1>No Products in Cart!</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>