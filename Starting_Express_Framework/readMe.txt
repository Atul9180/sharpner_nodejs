Node.js App using sequelize library to make sql connection more secure and reliable.
i.e It helps us focus more on Api logic rather than sql .


AppFlow:

app.js (entry point app) madee express server,setting bodyparser to parse form data, used app.set for ejs,
imported routes (admin,shop,contact,error) then used them using app.use.

2. for admin route:-> imported admincontroller(all fns) ,setting route endpoints with their fn. and method .

for shop route;
for contact route:
for error route:


==========================================